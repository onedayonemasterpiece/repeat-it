plugins { id 'java-library' }
java { toolchain { languageVersion = JavaLanguageVersion.of(17) } }
tasks.register('coreChecks', JavaExec) {
    dependsOn testClasses
    classpath = sourceSets.test.runtimeClasspath
    mainClass = 'com.onedayonemasterpiece.repeatit.core.CoreChecks'
    enableAssertions = true
}
tasks.register('presentationChecks', JavaExec) {
    dependsOn testClasses
    classpath = sourceSets.test.runtimeClasspath
    mainClass = 'com.onedayonemasterpiece.repeatit.core.PresentationChecks'
    enableAssertions = true
}
tasks.register('schedulerSimulationChecks', JavaExec) {
    dependsOn testClasses
    classpath = sourceSets.test.runtimeClasspath
    mainClass = 'com.onedayonemasterpiece.repeatit.core.SchedulerSimulationChecks'
    enableAssertions = true
}
check.dependsOn coreChecks, presentationChecks, schedulerSimulationChecks
